import React from "react";
import logo from "../images/FreeCodeCamp_logo.png"
const Logo = () => (
    <div className="freecodecamp-logo-container">
         <img
          src={logo}
          className="freecodecamp-logo"
        />
    </div>
);

export default Logo;